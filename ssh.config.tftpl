Host ${cluster_name}-cp
    HostName ${cluster_fqdn}
    User ${cluster_user}
    Port 2222

%{ for name, server in servers ~}
Host ${cluster_name}-${server.short_name}
    HostName ${cluster_name}
    User ${cluster_user}
    Port 2222
    ProxyJump ${cluster_name}-cp

%{ endfor ~}
